[project]
name = "bot"
version = "0.1.0"
description = "Slackbot"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-cloud-aiplatform[agent-engines]>=1.91.0",
    "slack-bolt>=1.23.0",
]
